let value = "Yuliia";
let toNumber = Number(value);
console.log(`${value}`, toNumber);
console.log(`${value}`, typeof toNumber);

toNumber = +value;
console.log(`${value}`, toNumber);
console.log(`${value}`, typeof toNumber);

value = "2222";
 toNumber = Number(value);
console.log(`${value}`, toNumber);
console.log(`${value}`, typeof toNumber);

toNumber = +value;
console.log(`${value}`, toNumber);
console.log(`${value}`, typeof toNumber);

value = "";
 toNumber = Number(value);
console.log(`${value}`, toNumber);
console.log(`${value}`, typeof toNumber);

toNumber = +value;
console.log(`${value}`, toNumber);
console.log(`${value}`, typeof toNumber);

value = " ";
 toNumber = Number(value);
console.log(`${value}`, toNumber);
console.log(`${value}`, typeof toNumber);

toNumber = +value;
console.log(`${value}`, toNumber);
console.log(`${value}`, typeof toNumber);

value = true;
toNumber = Number(value);
console.log(`${value}`, toNumber);
console.log(`${value}`, typeof toNumber);

value = true;
toNumber = +value;
console.log(`${value}`, toNumber);
console.log(`${value}`, typeof toNumber);

value = false;
toNumber = Number(value);
console.log(`${value}`, toNumber);
console.log(`${value}`, typeof toNumber);

value = false;
toNumber = +value;
console.log(`${value}`, toNumber);
console.log(`${value}`, typeof toNumber);

value = undefined;
toNumber = Number(value);
console.log(`${value}`, toNumber);
console.log(`${value}`, typeof toNumber);

value = undefined;
toNumber = +value;
console.log(`${value}`, toNumber);
console.log(`${value}`, typeof toNumber);

value = null;
toNumber = Number(value);
console.log(`${value}`, toNumber);
console.log(`${value}`, typeof toNumber);

value = null;
toNumber = +value;
console.log(`${value}`, toNumber);
console.log(`${value}`, typeof toNumber);